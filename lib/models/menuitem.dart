import 'package:flutter/material.dart';

class MenuItem {
  String title;
  String description;
  Color bgColor;
  IconData icon;

  MenuItem({
    required this.title,
    required this.description,
    required this.bgColor,
    required this.icon,
  });
}
